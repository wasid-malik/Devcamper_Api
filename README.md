# Devcamper_Api
A backend Api for Bootcamp directory application
make config/config.env file and update values to your own

# INSTALL DEPENDENCIES
npm install

# Run app
npm run dev (in development mode)
npm start (in production mode)

# Database seeder 
node seeder -d (destroy all data)
node seeder -i (import all data)
